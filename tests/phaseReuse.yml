phases:
- template: phaseReuse2.yml  # Template reference
  parameters:
    name: macOS
    queue: Hosted macOS Preview

- template: phaseReuse2.yml  # Template reference
  parameters:
    name: Linux
    queue: Hosted Linux Preview

- template: phaseReuse2.yml  # Template reference
  parameters:
    name: Windows
    queue: Hosted VS2017
    sign: true  # Extra step on Windows only
